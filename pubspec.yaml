name: sharingbridge_mobile_app
description: SharingBridge mobile app scaffold for donor setup flow.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.6
  google_sign_in: ^6.2.2
  geolocator: ^13.0.2
  image_picker: ^1.1.2
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0

flutter:
  uses-material-design: true
